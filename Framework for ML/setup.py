from setuptools import setup,find_packages

setup(
    name='framework for ml',
    version='1.0',
    long_description=__doc__,
    include_package_data=True,
    zip_safe=False,
    packages=find_packages()
)